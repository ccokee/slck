apiVersion: v1
kind: Service
metadata:
  name: {{ include "slck-chart.fullname" . }}-redis
  labels:
    {{- include "slck-chart.labels" . | nindent 4 }}
    app: redis
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
